interface:
  display_name: "GrillPowers"
  short_description: "From hard product questions to verified delivery"
  default_prompt: "Use $grill-powers to clarify this idea, approve a specification, and carry it through verified delivery."

policy:
  allow_implicit_invocation: false
